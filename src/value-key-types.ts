export enum TemplateKeyType {
  TYPE_ARRAY,
  DUPLICATE,
  SIMPLE,
  TEMPLATE,
  IF,
  KEEP,
  PRESERVE_EMPTY_ARRAYS
}

export enum TemplateValueType {
  LITERAL_NUMBER,
  LITERAL_STRING,
  LITERAL_BOOLEAN,
  JSON_PATH,
  ARRAY,
  OBJECT,
  FUNCTION,
  UNDEFINED
}
